using System;
using UnityEngine;

namespace Tarea
{
    public class Comp3 : MonoBehaviour
    {
        private void Awake()
        {
            
        }

        // Start is called before the first frame update
        void Start()
        {
            Comp1.miObjeto.name = "NuevoNombreDeCubo";
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
